name: classroom-folder
deployment_template:
  type: terraform
  source: "./terraform-classroom-folder"
  #actuation_sa_role: "roles/owner"
  input_parameters:
    - name: parent_folder_id
      type: string
    - name: teacher_email
      type: string
  output_parameters:
    - name: folder_id
      type: string
